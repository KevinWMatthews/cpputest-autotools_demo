Taken and modified from https://github.com/mpoullet/autotools-cpputest

This assumes that CppUTest is available on the system.
If it is installed in a custom location, run ./configure with the --prefix command.
